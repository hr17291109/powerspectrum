// binning info to match to the BOSS multipoles (Delta k = 0.001h/Mpc, up to k=0.4h/Mpc)
#pragma once
constexpr double kmin = 0.;
constexpr double kmax = 0.4;
constexpr int nbins = 400;
constexpr bool logbin = false;
